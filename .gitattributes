# 统一换行符：仓库内一律 LF，避免 Windows / macOS 协作时出现整文件 diff
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则可能执行异常
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制资源不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
